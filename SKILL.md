---
name: concept-learn
description: 分阶段讲解某个概念或技术，只有当用户在当前阶段理解后才进入下一阶段。当用户表达系统性学习意图（如"教我X"、"我想学X"、"X的原理是什么"）或显式调用 /concept-learn 时触发。
---

# Concept Learn（分阶段概念学习）

本技能用于系统性、分阶段地讲解某个概念或技术。核心原则：**只有当用户在当前阶段理解后，才进入下一阶段**。支持存档与恢复，跨会话延续学习进度。

---

## 一、触发条件

满足以下任一条件即触发：

1. **显式调用**：
   - `/concept-learn` —— 回复"你想学什么？请告诉我主题名称。"
   - `/concept-learn <主题>` —— 直接以该主题进入执行前提问。
   - `/concept-learn 恢复` —— 列出所有可恢复的存档列表。
   - `/concept-learn 恢复 <主题>` —— 查找并匹配对应主题存档，向用户确认后恢复。

2. **系统性学习意图关键词**：用户表达中包含以下意图——
   - "教我 X"、"我想学 X"、"带我掌握 X"
   - "X 的原理是什么"

**不触发的情况**（直接回答即可）：
- 一次性查询："X 是什么"、"X 怎么用"、"X 为什么报错"
- 求助式提问："帮我看看这个 X 的错误"、"帮我解决一下 X 的问题"
- 应试提问："X 的考点有哪些"、"X 常考题"

**判断标准**：用户是否明确表达**分步骤、按阶段**学习的意图。凡是一次性能回答完的问题，都不触发。

**特殊规则**：即使用户口语化表达像一次性提问，只要显式调用了 `/concept-learn`，就进入分阶段教学模式。

### 主题提取规则

1. **显式命令带参数**：`/concept-learn <参数>`，参数中可能包含自然语言。
   - 如果参数包含触发关键词（"教我"、"我想学"、"带我掌握"），用关键词后面的内容作为主题。
   - 如果不含关键词，直接以参数全部内容作为主题。
   - 示例：
     - `/concept-learn 我想学 python` → 主题 = python
     - `/concept-learn 我想学 python 的 async 原理` → 主题 = python async 原理
     - `/concept-learn React` → 主题 = React
2. **自然语言触发**：从句子中提取主题，规则同上。
3. **多主题**：检测到多个主题（如"我想学 React 和 Vue"），回复"你提到多个主题，请先选择一个开始学习"，让用户选择。
4. **slug 化**：保存时转小写、空格转连字符、去除特殊字符。

---

## 二、执行前提问（3 个必答问题）

触发后、列出大纲前，必须先向用户提出以下 3 个问题。**一次只问一个**，等用户回答后再问下一个。每个问题都提供推荐默认值，用户回"默认"可快速通过。

> **用户一次性回答多个问题时**：只取第一个答案，忽略其他。回复："收到，已记录。接下来回答第二个问题：..."
>
> **无效选择处理**：用户回复的序号不在选项范围内（如选项 1/2/3，用户输入 4），提示："无效选项，请重新选择（输入序号 1/2/3，或回"默认"）。"。连续 3 次输入无效选项 → 退出整个 concept-learn 学习流程，回复："已超过重试次数，concept-learn 已退出。"

### 问题 1：学习目标导向

问："你学这个概念/技术是为了什么目的？（请选择序号）"

```
1. 应用导向（默认）—— 学完就能上手用，侧重实操和代码示例
2. 理解导向 —— 学透原理和设计思想，侧重概念演进和设计权衡
3. 应试/面试导向 —— 学完应对考核场景，侧重高频考点和标准答案
```

回复数字序号 1/2/3，或回"默认"选择 1。

### 问题 2：解释详略

问："每个阶段的内容讲到多深、多细？（请选择序号）"

```
1. 精炼要点 —— 只讲核心定义、关键规则、最小可用示例
2. 详尽展开（默认）—— 核心定义 + 多个示例 + 类比解释 + 边界情况 + 常见误区
```

回复数字序号 1/2，或回"默认"选择 2。

### 问题 3：是否自检

问："每个阶段讲完后，要不要出题让你检验理解？（请选择序号）"

```
1. 出题自检（默认）—— 阶段讲完后出 1-3 道原理题，答对才进入下一阶段
2. 只讲解不考核 —— 阶段讲完后直接口头确认，说"懂了"就继续
```

回复数字序号 1/2，或回"默认"选择 1。

---

## 三、列出大纲并确认

3 个问题回答完毕后，根据用户的学习目标、详略偏好，生成完整学习大纲。

### 大纲格式

```
学习主题：<主题名>
学习目标导向：<应用/理解/应试>
解释详略：<精炼/详尽>
是否自检：<是/否>

大纲：
第 1 阶段：<阶段标题> —— <一句话简述>
第 2 阶段：<阶段标题> —— <一句话简述>
第 3 阶段：<阶段标题> —— <一句话简述>
...
```

### 确认与调整流程

列出大纲后，**必须**询问用户："这份大纲整体满意，还是其中几个阶段需要调整？"

- **若用户说整体不满意** → 重新生成完整大纲，再次确认。
- **若用户指出个别阶段需调整**（如"第 3 阶段拆成两个"、"第 2 阶段换顺序"、"加一个关于 X 的阶段"）→ 只改指定项，其余保留，再次确认。
- **循环直至用户明确确认**后，方可进入第一阶段讲解。

**明确确认的等价表达集合**（以下任一即视为确认，进入第一阶段）：
- "大纲没问题"、"大纲可以"、"大纲可以了"
- "可以开始"、"开始吧"、"开始学习"
- "ok"、"OK"、"好的"、"行"、"行吧"
- "就这样"、"就这样吧"
- "确认"、"确认通过"

**不视为确认，需要继续追问的情况**：
- "我想想"、"等一下"、"再看看"
- "第二个阶段能不能改一下"（触发调整流程，不是确认）
- 用户没有针对大纲确认问题进行回复（如岔开话题）

**硬性规则**：未得到用户对大纲的明确确认，绝不允许进入讲解阶段。

---

## 四、逐阶段讲解

大纲确认后，从第 1 阶段开始讲解。

### 讲解规则

- 严格按大纲顺序，一次只讲一个阶段。
- 讲解内容严格遵守用户选择的"解释详略"设置。
- 讲解内容严格遵守用户选择的"学习目标导向"侧重。
- 每个阶段讲解开头标注当前阶段编号和标题，如"## 第 2 阶段：组件与 props"。
- 技术术语和代码示例保持英文原貌，不强行翻译。
- **可视化辅助**：必要时输出流程图、架构图、时序图、交互图等辅助理解。优先使用 Mermaid 语法；若平台不支持 Mermaid，则使用 ASCII 字符画替代。

### 阶段间放行机制

**自检开启时**：
1. 阶段讲完后，出 1-3 道**原理题**，让用户用语言描述回答，AI 判断是否正确。
2. 若**全部答错**（没有一道通过），询问用户："这些内容是否需要重新讲解？"
   - 用户确认"需要" → 换不同的描述方式或角度重新讲解当前阶段，然后再出自检题。
   - 用户说"不需要" → 直接进入下一阶段。
   - 重新讲解后再出题，如果还是全部答错，重复上述流程。
3. 用户答对（至少通过一道题）→ 进入下一阶段。
4. 用户显式说"跳过自检" → 跳过当前阶段的自检，直接进入下一阶段。
5. 用户显式说"关闭自检" → 后续所有阶段不再自检，按"自检关闭"模式运行。

**自检关闭时**：
- 阶段讲完后，询问"这部分理解了吗？有疑问吗？"。
- 用户说"懂了"、"明白了"、"继续"等 → 进入下一阶段。
- 用户提出疑问 → 解答疑问，再次确认。

### 中途修改设置

用户可随时开启或关闭自检：
- "开启自检" → 后续阶段按自检模式运行。
- "关闭自检" → 后续阶段跳过自检，改为口头确认。

### 跳过阶段

用户可说"第 3 阶段我已会，跳过" → 将该阶段标记为 `completed`，`current_stage` 跳到下一个未完成阶段的编号。可复习被跳过的阶段，复习时重新完整讲解该阶段内容，完成后回到当前进度，不改变 `current_stage`。

### 复习已学阶段

用户可说"复习一下第 1 阶段" → 重新讲解该阶段内容，复习完后回到当前进度继续。复习不改变 `current_stage`。

### 用户岔开话题的处理

- 用户提出非学习相关的问题 → 正常回答该问题。
- 回答完后回到学习中，提示："回到学习中，刚刚我们讲到 [当前阶段名]……"。
- 不中断学习状态，`current_stage` 保持不变，不触发退出提示或存档询问。

**用户询问进度**：
- 用户可随时说"刚刚讲到哪了"、"之前教到哪了"、"现在学到哪了"等，AI 回复当前阶段信息：
  "当前在第 [N] 阶段（[阶段标题]），[已完成/进行中]。"

---

## 五、存档系统

### 存档目录结构

在当前工作区下创建 `.concept-learn` 目录，每个学习主题一个子目录：

```
当前工作区/
  .concept-learn/
    react/
      save.md
    vue/
      save.md
    cpp-stl/
      save.md
```

子目录名 = 主题名 slug 化（转小写、空格转连字符、去除特殊字符）。

### 存档文件格式

每个主题子目录下有一个 `save.md` 文件，格式为 **Markdown + YAML frontmatter**：

```markdown
---
topic: React
goal: 应用
detail_level: 详尽
self_check: true
outline:
  - stage: 1
    title: JSX 语法
    summary: JSX 的基本语法和规则
  - stage: 2
    title: 组件与 props
    summary: 组件定义、props 传递
current_stage: 3
stage_status:
  - stage: 1
    status: completed
  - stage: 2
    status: completed
  - stage: 3
    status: in_progress
  - stage: 4
    status: not_started
created_at: 2026-07-26
updated_at: 2026-07-26
---

# React 学习存档

## 大纲
1. JSX 语法 —— JSX 的基本语法和规则
2. 组件与 props —— 组件定义、props 传递
3. state 与生命周期 —— 状态管理与生命周期钩子
4. hooks —— useState、useEffect 等核心 hooks

## 第 1 阶段：JSX 语法（已完成）
<完整讲解内容>

## 第 2 阶段：组件与 props（已完成）
<完整讲解内容>

## 第 3 阶段：state 与生命周期（进行中）
<已讲解的部分内容>

## 自检记录
- 第 1 阶段：通过
- 第 2 阶段：跳过
```

### 存档字段说明

| 字段 | 含义 |
|------|------|
| `topic` | 学习主题（原始名称，如 "React"） |
| `goal` | 学习目标导向（应用/理解/应试） |
| `detail_level` | 解释详略（精炼/详尽） |
| `self_check` | 是否自检（true/false），持久化，支持中途切换 |
| `outline` | 完整大纲（阶段列表，每阶段含编号、标题、简述） |
| `current_stage` | 当前所在阶段编号（从 1 开始） |
| `stage_status` | 每个阶段的状态（not_started/in_progress/completed） |
| `created_at` | 存档创建时间（YYYY-MM-DD） |
| `updated_at` | 最近更新时间（YYYY-MM-DD） |

### 阶段状态机

- `not_started`：该阶段未开始讲解。
- `in_progress`：该阶段讲解中（用于中途存档）。
- `completed`：该阶段已讲完。**讲完即 completed，阶段内容保存在正文中。**

### 恢复时 `current_stage` 和 `stage_status` 的优先级规则

1. **以 `stage_status` 为主，`current_stage` 仅作参考。**
2. 恢复时按以下顺序查找：
   - 优先找状态为 `in_progress` 的阶段中最小编号者。
   - 如果没有 `in_progress`，找状态为 `not_started` 的阶段中最小编号者。
   - 如果全是 `completed`，则学习已完成，输出完整内容总结后删除存档。
3. 找到继续阶段后，**同步更新 `current_stage` 为该阶段编号**。
4. 如果出现多个 `in_progress`（异常状态），取最小编号，并提示用户"检测到存档异常，已从第 N 阶段恢复"。

### 主动存档

用户可随时主动触发存档，触发词包括："保存进度"、"存档"等。

触发后：
1. 将当前进度写入 `.concept-learn/<主题slug>/save.md`（覆盖旧文件）。
2. 更新 `updated_at` 字段。
3. 回复确认："已存档至 .concept-learn/<主题slug>/save.md，当前进度：第 N 阶段（<阶段标题>）。"

### 退出时提示

当用户显式表达退出意图（触发词："退出"、"结束学习"、"不学了"、"今天就到这"、"下次再学"、"先保存"等）时：

1. 检查当前是否有未保存的进度。

**"未保存进度"的判定标准：**
- **已有存档**：自上次存档后，有任意阶段的 `stage_status` 发生了改变（如从 `not_started` 变成 `completed`，或从 `not_started` 变成 `in_progress`）。
- **无存档（首次学习）**：只要进入了任何阶段的讲解（`current_stage > 0`），视为有未保存进度。
- **还在大纲确认阶段就退出**：视为无未保存进度，直接退出，不询问。

2. 若有未保存进度，提示："你当前的学习进度尚未保存，是否需要保存？"
3. 提供三个选项：
   - **是** → 执行存档，然后退出。
   - **否** → 不存档，直接退出。
   - **取消退出** → 不存档，继续学习。

**只响应显式退出信号**，不因用户临时岔开话题而触发退出提示。

---

## 六、恢复存档

### 恢复触发

支持两种触发方式：

1. **显式命令**：
   - `/concept-learn 恢复` → 列出所有可恢复的存档列表。
   - `/concept-learn 恢复 <主题>` → 查找并匹配对应主题存档，向用户确认后恢复。
2. **自然语言**：用户说"继续上次学的 X"、"恢复 X 进度"、"我之前学到哪了"、"接着学 X" 等。

### 恢复流程

1. **用户通过命令指定了主题**（`/concept-learn 恢复 <主题>`）：
   - 查找 `.concept-learn/` 下对应主题的 slug 子目录。
   - 若找到 → 向用户确认："是否恢复 [主题] 的存档？"，确认后恢复。
   - 若未找到 → 告知用户没有该主题的存档，询问是否要新建学习。

2. **用户通过自然语言指定了主题**：
   - 在 `.concept-learn/` 下查找对应主题的子目录。
   - 若找到 → 直接恢复（不需要二次确认）。
   - 若未找到 → 告知用户没有该主题的存档，询问是否要新建学习。

3. **用户未指定主题**（如"继续上次的学习"）：
   - 扫描 `.concept-learn/` 下所有子目录。
   - **若只有一个存档** → 直接恢复那个。
   - **若有多个存档** → 列出所有存档主题及当前进度，让用户选一个：
     ```
     找到以下学习存档：
     1. React —— 当前第 3 阶段（state 与生命周期）
     2. Vue —— 当前第 2 阶段（组件基础）
     请选择要继续的存档（输入序号或主题名）。
     ```
   - **若无任何存档** → 告知用户没有可恢复的存档，询问是否要新建学习。

### 恢复后行为

1. 读取存档的 frontmatter 获取结构化数据（goal、detail_level、self_check、outline、current_stage、stage_status）。
2. 读取正文获取已讲解的阶段内容。
3. 向用户简要汇报恢复状态：
   ```
   已恢复学习存档：
   主题：React
   学习目标：应用导向
   详略：详尽
   当前进度：第 3 阶段（state 与生命周期）—— 进行中

   将从第 3 阶段继续讲解。
   ```
4. 找到 `stage_status` 为 `in_progress` 或 `not_started` 的当前阶段（按优先级规则），从该阶段继续讲解。
   - 若当前阶段是 `in_progress` → 从中断处继续讲解剩余内容。
   - 若当前阶段是 `not_started` → 从头讲解该阶段。

**注意**：恢复存档时，跳过执行前提问（3 个必答问题），直接进入恢复状态汇报和讲解。

---

## 七、完整执行流程总结

```
用户触发
  ↓
[执行前提问] 3 个必答问题（目标导向 → 详略 → 自检），一次一问
  ↓
[列出大纲] 根据用户选择生成大纲
  ↓
[大纲确认] 询问整体满意还是个别调整 → 循环至确认
  ↓
[第一阶段讲解] 按详略和目标导向讲解
  ↓
[阶段放行] 自检开启则出原理题考核，自检关闭则口头确认
  ↓
[进入下一阶段] 循环讲解与放行
  ↓
（中途可触发：开启/关闭自检 / 跳过阶段 / 复习 / 主动存档 / 岔开话题）
  ↓
[所有阶段完成] → 输出内容总结 → 恭喜完成 → 删除存档
  ↓
[用户退出] → 检查未保存进度 → 提示是否存档 → 存档或直接退出
  ↓
（新会话）[恢复存档] → 跳过 3 问 → 从中断处继续
```

---

## 八、语言规则

- 与用户的所有交互使用中文。
- SKILL.md 正文用中文为主，技术术语和代码示例保持英文原貌。
- 存档文件内容用中文，技术术语保持英文。
- 自检题目用中文表述，代码类题目中的代码保持英文。

---

## 九、注意事项

1. **严格分阶段**：绝不一次性讲完所有内容，必须按阶段逐步推进。
2. **严格确认**：大纲未确认不许讲解；阶段未通过不许进入下一阶段（自检开启时）。
3. **存档覆盖**：每次存档覆盖旧文件，不保留历史版本。
4. **自检持久化**：自检设置（self_check）持久化到存档，自检通过状态不持久化。
5. **用户始终可控**：用户可随时跳过自检、关闭自检、跳过阶段、复习、存档、退出。
