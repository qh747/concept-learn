---
name: concept-learn
description: 分阶段讲解某个概念或技术，只有当用户在当前阶段理解后才进入下一阶段。当用户表达系统性学习意图（如"教我X"、"我想学X"、"带我掌握X"、"从零开始学X"）或显式调用 /concept-learn 时触发。
---

# Concept Learn（分阶段概念学习）

本技能用于系统性、分阶段地讲解某个概念或技术。核心原则：**只有当用户在当前阶段理解后，才进入下一阶段**。支持存档与恢复，跨会话延续学习进度。

---

## 一、触发条件

满足以下任一条件即触发：

1. **显式调用**：用户输入 `/concept-learn` 或 `/concept-learn <主题>`。
2. **系统性学习意图关键词**：用户表达中包含以下意图——"教我 X"、"我想学 X"、"带我掌握 X"、"从零开始学 X"、"系统学习 X"、"入门到精通 X" 等。

**不触发的情况**：一次性具体提问（如"X 怎么用"、"X 是什么意思"）。这类问题直接回答，不进入分阶段教学流程。

**特殊规则**：即使用户口语化表达像一次性提问，只要显式调用了 `/concept-learn`，就进入分阶段教学模式。

---

## 二、执行前提问（3 个必答问题）

触发后、列出大纲前，必须先向用户提出以下 3 个问题。**一次只问一个**，等用户回答后再问下一个。每个问题都提供推荐默认值，用户回"默认"可快速通过。

### 问题 1：学习目标导向

问："你学这个概念/技术是为了什么目的？"

- **应用导向**（默认）：学完就能上手用。讲解侧重实操、代码示例、常见用法。
- **理解导向**：学透原理和设计思想。讲解侧重概念演进、设计权衡、与其他方案对比。
- **应试/面试导向**：学完应对考核场景。讲解侧重高频考点、易错点、标准答案表述。

### 问题 2：解释详略

问："每个阶段的内容讲到多深、多细？"

- **精炼要点**：只讲核心定义、关键规则、最小可用示例。
- **详尽展开**（默认）：核心定义 + 多个示例 + 类比解释 + 边界情况 + 常见误区 + 与相关概念对比。

### 问题 3：是否自检

问："每个阶段讲完后，要不要出题让你检验理解？"

- **出题自检**（默认）：阶段讲完后出 1-3 道题（选择/简答/让用户复述），用户答对才进入下一阶段。
- **只讲解不考核**：阶段讲完后直接问"理解了吗？有疑问吗？"，用户说"懂了"或"继续"就进入下一阶段。

---

## 三、列出大纲并确认

3 个问题回答完毕后，根据用户的学习目标、详略偏好，生成完整学习大纲。

### 大纲格式

```
学习主题：<主题名>
学习目标导向：<应用/理解/应试>
解释详略：<精炼/详尽>
是否自检：<是/否>

大纲：
第 1 阶段：<阶段标题> —— <一句话简述>
第 2 阶段：<阶段标题> —— <一句话简述>
第 3 阶段：<阶段标题> —— <一句话简述>
...
```

### 确认与调整流程

列出大纲后，**必须**询问用户："这份大纲整体满意，还是其中几个阶段需要调整？"

- **若用户说整体不满意** → 重新生成完整大纲，再次确认。
- **若用户指出个别阶段需调整**（如"第 3 阶段拆成两个"、"第 2 阶段换顺序"、"加一个关于 X 的阶段"）→ 只改指定项，其余保留，再次确认。
- **循环直至用户明确确认**"大纲没问题"、"可以开始"等，方可进入第一阶段讲解。

**硬性规则**：未得到用户对大纲的明确确认，绝不允许进入讲解阶段。

---

## 四、逐阶段讲解

大纲确认后，从第 1 阶段开始讲解。

### 讲解规则

- 严格按大纲顺序，一次只讲一个阶段。
- 讲解内容严格遵守用户选择的"解释详略"设置。
- 讲解内容严格遵守用户选择的"学习目标导向"侧重。
- 每个阶段讲解开头标注当前阶段编号和标题，如"## 第 2 阶段：组件与 props"。
- 技术术语和代码示例保持英文原貌，不强行翻译。

### 阶段间放行机制

**自检开启时**：
1. 阶段讲完后，出 1-3 道自检题（选择/简答/复述）。
2. 用户答对 → 进入下一阶段。
3. 用户答错 → 针对错误点补充讲解，然后重新出题或换题。
4. 用户显式说"跳过自检" → 跳过当前阶段的自检，直接进入下一阶段。
5. 用户显式说"关闭自检" → 后续所有阶段不再自检，按"自检关闭"模式运行。

**自检关闭时**：
- 阶段讲完后，询问"这部分理解了吗？有疑问吗？"。
- 用户说"懂了"、"明白了"、"继续"等 → 进入下一阶段。
- 用户提出疑问 → 解答疑问，再次确认。

### 中途修改设置

用户可随时修改之前的设置：
- "改成精炼模式" / "改成详尽模式" → 后续阶段按新详略设置执行。
- "开启自检" / "关闭自检" → 后续阶段按新自检设置执行。
- "我想调整大纲" → 暂停当前讲解，回到大纲调整流程，确认后从调整位置继续。

### 跳过阶段

用户可说"第 3 阶段我已会，跳过" → 将该阶段标记为已完成，直接进入下一阶段。

### 复习已学阶段

用户可说"复习一下第 1 阶段" → 重新讲解该阶段内容，复习完后回到当前进度继续。复习不改变 `current_stage`。

---

## 五、存档系统

### 存档目录结构

在当前工作区下创建 `.concept-learn` 目录，每个学习主题一个子目录：

```
当前工作区/
  .concept-learn/
    react/
      save.md
    vue/
      save.md
    cpp-stl/
      save.md
```

子目录名 = 主题名 slug 化（转小写、空格转连字符、去除特殊字符）。

### 存档文件格式

每个主题子目录下有一个 `save.md` 文件，格式为 **Markdown + YAML frontmatter**：

```markdown
---
topic: React
goal: 应用
detail_level: 详尽
self_check: true
outline:
  - stage: 1
    title: JSX 语法
    summary: JSX 的基本语法和规则
  - stage: 2
    title: 组件与 props
    summary: 组件定义、props 传递
current_stage: 3
stage_status:
  - stage: 1
    status: completed
  - stage: 2
    status: completed
  - stage: 3
    status: in_progress
  - stage: 4
    status: not_started
created_at: 2026-07-26
updated_at: 2026-07-26
---

# React 学习存档

## 大纲
1. JSX 语法 —— JSX 的基本语法和规则
2. 组件与 props —— 组件定义、props 传递
3. state 与生命周期 —— 状态管理与生命周期钩子
4. hooks —— useState、useEffect 等核心 hooks

## 第 1 阶段：JSX 语法（已完成）
<完整讲解内容>

## 第 2 阶段：组件与 props（已完成）
<完整讲解内容>

## 第 3 阶段：state 与生命周期（进行中）
<已讲解的部分内容>

## 自检记录
- 第 1 阶段：通过
- 第 2 阶段：跳过
```

### 存档字段说明

| 字段 | 含义 |
|------|------|
| `topic` | 学习主题（原始名称，如 "React"） |
| `goal` | 学习目标导向（应用/理解/应试） |
| `detail_level` | 解释详略（精炼/详尽） |
| `self_check` | 是否自检（true/false），记录当前设置，支持中途切换 |
| `outline` | 完整大纲（阶段列表，每阶段含编号、标题、简述） |
| `current_stage` | 当前所在阶段编号（从 1 开始） |
| `stage_status` | 每个阶段的状态（not_started/in_progress/completed） |
| `stages_content` | 已讲解的阶段内容（正文 Markdown 部分） |
| `quiz_log` | 自检记录（每阶段的题目、用户作答、是否通过）—— 仅记录当轮会话，不持久化自检通过状态 |
| `created_at` | 存档创建时间（YYYY-MM-DD） |
| `updated_at` | 最近更新时间（YYYY-MM-DD） |

### 阶段状态机

- `not_started`：该阶段未开始讲解。
- `in_progress`：该阶段讲解中（用于中途存档）。
- `completed`：该阶段已讲完。**讲完即 completed，不存自检结果。** 自检是当轮会话内的门槛，不持久化。

### 主动存档

用户可随时主动触发存档，触发词包括："存档"、"保存进度"、"save" 等。

触发后：
1. 将当前进度写入 `.concept-learn/<主题slug>/save.md`（覆盖旧文件）。
2. 更新 `updated_at` 字段。
3. 回复确认："已存档至 .concept-learn/<主题slug>/save.md，当前进度：第 N 阶段（<阶段标题>）。"

### 退出时提示

当用户显式表达退出意图（触发词："退出"、"结束"、"不学了"、"结束学习" 等）时：

1. 检查当前是否有未保存的进度（自上次存档后有新讲解内容）。
2. 若有未保存进度，提示："你当前的学习进度尚未保存，是否需要保存？"
3. 提供三个选项：
   - **是** → 执行存档，然后退出。
   - **否** → 不存档，直接退出。
   - **取消退出** → 不存档，继续学习。

**只响应显式退出信号**，不因用户临时岔开话题而触发退出提示。

---

## 六、恢复存档

### 恢复触发

支持两种触发方式：

1. **显式命令**：用户输入 `/concept-learn 恢复` 或 `/concept-learn 继续`。
2. **自然语言**：用户说"继续上次学的 X"、"恢复 X 进度"、"我之前学到哪了"、"接着学 X" 等。

### 恢复流程

1. **用户指定了主题**（如"继续上次学的 React"）：
   - 在 `.concept-learn/` 下查找对应主题的子目录。
   - 若找到 → 读取 `save.md`，恢复进度。
   - 若未找到 → 告知用户没有该主题的存档，询问是否要新建学习。

2. **用户未指定主题**（如"继续上次的学习"）：
   - 扫描 `.concept-learn/` 下所有子目录。
   - **若只有一个存档** → 直接恢复那个。
   - **若有多个存档** → 列出所有存档主题及当前进度，让用户选一个：
     ```
     找到以下学习存档：
     1. React —— 当前第 3 阶段（state 与生命周期）
     2. Vue —— 当前第 2 阶段（组件基础）
     请选择要继续的存档（输入序号或主题名）。
     ```
   - **若无任何存档** → 告知用户没有可恢复的存档，询问是否要新建学习。

### 恢复后行为

1. 读取存档的 frontmatter 获取结构化数据（goal、detail_level、self_check、outline、current_stage、stage_status）。
2. 读取正文获取已讲解的阶段内容。
3. 向用户简要汇报恢复状态：
   ```
   已恢复学习存档：
   主题：React
   学习目标：应用导向
   详略：详尽
   当前进度：第 3 阶段（state 与生命周期）—— 进行中
   
   将从第 3 阶段继续讲解。
   ```
4. **关于自检**：恢复后重新询问用户本轮会话是否开启自检（因为自检状态不持久化）。
5. 找到 `stage_status` 为 `in_progress` 或 `not_started` 的当前阶段，从该阶段继续讲解。
   - 若当前阶段是 `in_progress` → 从中断处继续讲解剩余内容。
   - 若当前阶段是 `not_started` → 从头讲解该阶段。

---

## 七、完整执行流程总结

```
用户触发
  ↓
[执行前提问] 3 个必答问题（目标导向 → 详略 → 自检）
  ↓
[列出大纲] 根据用户选择生成大纲
  ↓
[大纲确认] 询问整体满意还是个别调整 → 循环至确认
  ↓
[第一阶段讲解] 按详略和目标导向讲解
  ↓
[阶段放行] 自检开启则出题考核，自检关闭则口头确认
  ↓
[进入下一阶段] 循环讲解与放行
  ↓
（中途可触发：修改设置 / 跳过阶段 / 复习 / 主动存档）
  ↓
[用户退出] → 提示是否存档 → 存档或直接退出
  ↓
（新会话）[恢复存档] → 从中断处继续
```

---

## 八、语言规则

- 与用户的所有交互使用中文。
- SKILL.md 正文用中文为主，技术术语和代码示例保持英文原貌。
- 存档文件内容用中文，技术术语保持英文。
- 自检题目用中文表述，代码类题目中的代码保持英文。

---

## 九、注意事项

1. **严格分阶段**：绝不一次性讲完所有内容，必须按阶段逐步推进。
2. **严格确认**：大纲未确认不许讲解；阶段未通过不许进入下一阶段（自检开启时）。
3. **存档覆盖**：每次存档覆盖旧文件，不保留历史版本。
4. **不存自检状态**：自检是当轮会话门槛，存档只记录讲解进度（completed = 讲完）。
5. **用户始终可控**：用户可随时跳过自检、关闭自检、跳过阶段、复习、修改设置、存档、退出。
