---
name: concept-learn
description: 分阶段讲解某个概念或技术，只有当用户在当前阶段理解后才进入下一阶段。当用户表达系统性学习意图（如"教我X"、"我想学X"、"X的原理是什么"）或显式调用 /concept-learn 时触发。
---

# Concept Learn（分阶段概念学习）

本技能用于系统性、分阶段地讲解某个概念或技术。核心原则：**只有当用户在当前阶段理解后，才进入下一阶段**。支持存档与恢复，跨会话延续学习进度。

## 全局最高原则

**任何场景向用户提问，一次只问一个问题。** 选项型问题一次只给一组选项。本原则覆盖本技能所有提问点：执行前提问、大纲确认、模板确认、阶段放行、自检答题判定、存档询问等。

---

## 一、触发条件

满足以下任一即触发：

1. **显式调用**：
   - `/concept-learn` —— 回复"你想学什么？请告诉我主题名称。"
   - `/concept-learn <主题>` —— 直接以该主题进入执行前提问。
   - `/concept-learn 恢复` —— 列出所有可恢复的存档列表。
   - `/concept-learn 恢复 <主题>` —— 查找并匹配对应主题存档，向用户确认后恢复。

2. **系统性学习意图关键词**：用户表达中包含"教我 X"、"我想学 X"、"带我掌握 X"、"X 的原理是什么"等明确分步骤学习的意图。

**不触发**（直接回答即可）：一次性查询（"X 是什么"/"X 怎么用"/"X 为什么报错"）、求助式提问、应试提问（"X 的考点有哪些"）。判断标准：用户是否明确表达**分步骤、按阶段**学习的意图。凡一次性能回答完的问题都不触发。

**特殊规则**：即使用户口语化表达像一次性提问，只要显式调用了 `/concept-learn`，就进入分阶段教学模式。

> 主题提取、多主题、slug 化等细节见 `references/edge-cases.md`。

---

## 二、执行流程总览

两条入口路径，进入讲解循环后行为一致：

**新触发会话**：用户触发 → 执行前提问（3 问 + 模板加载）→ 列出大纲 → 大纲确认（循环至明确确认）→ 逐阶段讲解循环 → 全部完成 → 内容总结 → 删除存档。

**恢复存档**：恢复触发 → 查找存档 → 读取存档并汇报状态 → **跳过执行前提问与模板加载** → 按 `stage_status` 优先级确定续讲阶段 → 进入讲解循环。

> 恢复流程细节见 `references/restore.md`；存档系统字段、状态机、优先级规则见 `references/save-system.md`。

---

## 三、执行前提问（3 个必答问题 + 模板加载）

触发后、列出大纲前，必须先依次提出 3 个问题。**一次只问一个**，等用户回答后再问下一个。每个问题提供推荐默认值，用户回"默认"可快速通过。

> 一次性多答、无效选择等异常处理见 `references/edge-cases.md`。

### 问题 1：学习目标导向

"你学这个概念/技术是为了什么目的？（请选择序号）"

```
1. 应用导向（默认）—— 学完就能上手用，侧重实操和代码示例
2. 理解导向 —— 学透原理和设计思想，侧重概念演进和设计权衡
3. 应试/面试导向 —— 学完应对考核场景，侧重高频考点和标准答案
```

回复 1/2/3，或回"默认"选 1。

### 模板加载流程（问题 1 选定导向后触发）

选定导向后，**读取该导向的模板索引** `templates/<导向>/_index.md`（application/understanding/exam），用用户原始表述匹配表中 keywords：

1. **唯一命中模板 X** → 提示"将采用模板 X" + **列出模板 X 的推荐大纲骨架** → 问："是否继续使用此模板？"
   - 同意 → 采用模板 X，进入大纲确认流程。
   - 不同意 → 展示该导向下其它可匹配模板列表，用户选定后采用；**按新模板重新生成大纲**再确认。
   - 仍不满意 → 提示可自定义模板（拷贝模板文件 + 在 `_index.md` 追加一行注册），然后退出学习流程。
2. **多命中** → 直接列出所有命中候选供用户选择；选定后按上述"同意/不同意"分支处理。
3. **零命中** → 列出该导向全部模板供用户选；若用户仍不选，提示可自定义模板后退出流程。

加载模板时**必须向用户提示加载了哪套模板**。模板文件位于 `templates/<导向>/<模板名>.md`，含推荐大纲骨架与各阶段讲解结构指导，agent 据此生成大纲与组织内容。

### 问题 2：解释详略

"每个阶段的内容讲到多深、多细？（请选择序号）"

```
1. 精炼要点 —— 只讲核心定义、关键规则、最小可用示例
2. 详尽展开（默认）—— 核心定义 + 多个示例 + 类比解释 + 边界情况 + 常见误区
```

回复 1/2，或回"默认"选 2。

### 问题 3：是否自检

"每个阶段讲完后，要不要出题让你检验理解？（请选择序号）"

```
1. 出题自检（默认）—— 阶段讲完后出 1-3 道原理题，答对才进入下一阶段
2. 只讲解不考核 —— 阶段讲完后直接口头确认，说"懂了"就继续
```

回复 1/2，或回"默认"选 1。

---

## 四、列出大纲并确认

3 个问题 + 模板加载完成后，根据用户的目标、详略偏好与所选模板的推荐骨架，生成完整学习大纲。

### 大纲格式

```
学习主题：<主题名>
学习目标导向：<应用/理解/应试>
解释详略：<精炼/详尽>
是否自检：<是/否>
采用模板：<模板名>

大纲：
第 1 阶段：<阶段标题> —— <一句话简述>
第 2 阶段：<阶段标题> —— <一句话简述>
...
```

### 确认与调整

列出大纲后，**必须询问**："这份大纲整体满意，还是其中几个阶段需要调整？"

- 整体不满意 → 重新生成完整大纲，再次确认。
- 个别阶段需调整 → 只改指定项，其余保留，再次确认。
- **循环至用户明确确认**后方可进入第一阶段讲解。

**视为确认**：明确肯定性短词（"可以"/"没问题"/"开始"/"确认"/"就这样"等）。犹豫、提具体调整、岔开话题均不视为确认。

**硬性规则**：未得到用户对大纲的明确确认，绝不允许进入讲解阶段。

---

## 五、逐阶段讲解

大纲确认后，从第 1 阶段开始讲解。

### 讲解规则

- 严格按大纲顺序，一次只讲一个阶段。
- 讲解内容严格遵守用户选择的"解释详略"与"学习目标导向"侧重，并遵循所加载模板的"各阶段讲解结构指导"。
- 每个阶段讲解开头标注当前阶段编号和标题，如"## 第 2 阶段：组件与 props"。
- 技术术语和代码示例保持英文原貌，不强行翻译。
- **可视化落地**：每阶段至少一个类比（把抽象机制映射到日常经验）；关键流程必有 Mermaid 流程图/时序图/状态图，平台不支持 Mermaid 时用 ASCII 字符画替代。

### 阶段内输出模式

`output_mode` 为全局持久化配置，跨会话保持。默认 `step`。

**分步输出（`step`）**：AI 将阶段拆为若干讲解点，**一次只输出一个讲解点**，用户确认后输出下一个。

- **分步切分原则**：按子概念切分，每点一个主题、控制篇幅；每点开头一句话点明该点要讲什么，结尾过渡到下一点。
- 讲解点之间的确认信号复用阶段间放行词（"懂了"/"明白了"/"继续"/"下一个"等）。
- 不在每个讲解点后触发自检；自检仅在阶段最后一个讲解点输出后执行。
- 用户切换为"全部输出"时，从当前讲解点继续输出本阶段剩余内容，不重复已讲部分，确认语："已切换为全部输出模式，本阶段剩余内容将一次性输出。"
- 用户切换为"分步输出"时，对下一阶段生效，当前阶段不切换，确认语："已切换为分步输出模式，将在下一阶段开始逐点讲解。"

**全部输出（`all`）**：AI 一次性输出当前阶段全部内容，完成后进入阶段放行流程。

**模式切换触发词**：
- 切全部输出："全部输出"/"全部讲完"/"一次讲完"/"一次输出"/"全部展开"/"不用分步"/"跳过分步"
- 切分步输出："分步输出"/"一个一个来"/"逐步讲"/"分点讲"/"回到分步"

**首次进入提示**：只在第一阶段开始讲解前提示一次——告知当前为分步输出模式、用户确认后讲下一点、说"全部输出"可切换为一次输出。之后不再重复。

### 阶段间放行机制

**自检开启时**：
1. 阶段讲完后，出 1-3 道**原理题**，让用户用语言描述回答，AI 判断是否正确。
2. **全部答对才放行**，进入下一阶段。
3. 未全对 → 询问："这些内容是否需要重新讲解？"
   - 用户说"需要" → 换不同的描述方式或角度重新讲解当前阶段，然后再出自检题。
   - 用户说"不需要" → 直接进入下一阶段。
   - 重新讲解后再出题仍未全对，重复上述流程。
4. 用户显式"跳过自检" → 跳过当前阶段自检直接进下一阶段（不影响后续阶段自检）。
5. 用户显式"关闭自检" → 后续所有阶段不再自检，按"自检关闭"模式运行。

**自检关闭时**：
- 阶段讲完后，询问"这部分理解了吗？"。用户说"懂了"/"明白了"/"继续"等 → 进入下一阶段。用户提出疑问 → 解答后再次确认。

### 中途修改设置

- "开启自检" → 后续阶段按自检模式运行。
- "关闭自检" → 后续阶段跳过自检改为口头确认。

> 跳过阶段、复习已学阶段、用户岔开话题、询问进度等处理见 `references/edge-cases.md`。

---

## 六、存档与恢复

### 存档

存档目录为当前工作区下 `.concept-learn/<主题slug>/save.md`，格式为 Markdown + YAML frontmatter，含 frontmatter 结构化字段与正文已讲内容。每次存档覆盖旧文件，不保留历史版本。

**主动存档**：用户说"保存进度"/"存档"等 → 写入存档 → 更新 `updated_at` → 回复确认进度。

**退出时提示**：用户显式退出（"退出"/"结束学习"/"不学了"/"今天就到这"/"下次再学"/"先保存"等）时检查未保存进度：有则询问是否保存（是/否/取消退出）；无则直接退出。只响应显式退出信号。

> 存档字段表、阶段状态机（not_started/in_progress/completed）、恢复优先级规则、未保存进度判定标准见 `references/save-system.md`。

### 恢复

**触发方式**：显式命令 `/concept-learn 恢复`（列存档）、`/concept-learn 恢复 <主题>`（匹配并确认）；自然语言"继续上次学的 X"/"恢复 X 进度"/"接着学 X"等。

**恢复后行为**：读取存档汇报状态（主题/目标/详略/输出模式/模板/进度）→ **跳过执行前提问与模板加载** → 按 `stage_status` 优先级确定续讲阶段（`in_progress` 从中断处续讲，`not_started` 从头讲）。

> 恢复流程细节见 `references/restore.md`。

---

## 七、语言规则

- 与用户所有交互使用中文。SKILL.md 正文中文为主，技术术语和代码示例保持英文原貌。
- 存档文件内容用中文，技术术语保持英文。自检题目用中文，代码类题目中的代码保持英文。

---

## 八、注意事项

1. **严格分阶段**：绝不一次性讲完所有内容，按阶段逐步推进。
2. **严格确认**：大纲未确认不许讲解；自检开启时未全对不许进入下一阶段。
3. **一次只问一个**：任何提问点一次只问一个问题、一组选项。
4. **存档覆盖**：每次存档覆盖旧文件，不保留历史版本。
5. **持久化**：`self_check` 与 `output_mode` 持久化到存档，跨会话保持；自检通过状态不持久化。
6. **用户始终可控**：可随时跳过自检、关闭自检、切换输出模式、跳过阶段、复习、存档、退出。